export const PUBLIC_PATH = "/";

export const routesConfig = {
    HOME: `${PUBLIC_PATH}`,
    ADD_POST: `${PUBLIC_PATH}add-post`,
    SIGNIN: `${PUBLIC_PATH}auth/sign-in`,
    SIGNUP: `${PUBLIC_PATH}auth/sign-up`,
    ABOUT: `${PUBLIC_PATH}about`,
    NOT_FOUND: `${PUBLIC_PATH}:patchMath(.*)*`,
}